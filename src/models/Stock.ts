export interface Stock {
  name: string;
  id: number;
  title: string;
  brand: string;
  description: string;
  price: number;
  rating: number;
  discountPercentage: string;
  stock: number;
  thumbnail: string;
  images: string[];
  // Add other properties as needed
}
